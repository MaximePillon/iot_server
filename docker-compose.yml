version: '3'

services:
  postgresql:
    image: 'bitnami/postgresql:latest'
    ports:
      - '5432:5432'
    environment:
      - POSTGRESQL_DATABASE=iotproject
      - POSTGRESQL_USERNAME=username
      - POSTGRESQL_PASSWORD=password